apply plugin: "java"
apply plugin: "application"

mainClassName = "capstat.HelloWorld"

repositories {
    mavenCentral()
}

dependencies {

}

jar {
    baseName = "capstat"
    version = "0.0.1"
    manifest {
        attributes "Main-Class": "$mainClassName"
    }
}
